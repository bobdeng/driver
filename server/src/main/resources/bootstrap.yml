spring:
  application:
    name: server
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:9000/config
      name: server,db,redis
---
spring:
  profiles: release
  cloud:
    config:
      uri: http://config.local/config
      name: server,db,redis
---
spring:
  profiles: test
  cloud:
    config:
      uri: http://config-test.local/config
      name: server,db,redis